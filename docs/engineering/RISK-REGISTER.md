# Risk Register - Altora Resto

Status dokumen: **DRAF AWAL**. Skala Likelihood/Impact: `RENDAH`, `SEDANG`, `TINGGI`.
Skor prioritas = kombinasi keduanya (`TINGGI x TINGGI` = prioritas tertinggi).

| ID | Risiko | Domain | Kemungkinan | Dampak | Mitigasi | Status |
|---|---|---|---|---|---|---|
| RISK-001 | Query lolos tanpa filter `tenantId`/`outletId`, bocor data lintas tenant | Platform/lintas domain | SEDANG | TINGGI | Wajib helper query terpusat di `packages/tenant` yang menyuntikkan filter tenant; test integrasi wajib untuk setiap service baru | TERBUKA |
| RISK-002 | Race condition pemotongan stok saat banyak pesanan bersamaan untuk item dengan stok terbatas | Persediaan/Pesanan | SEDANG | TINGGI | Transaksi DB dengan row lock saat menulis `MutasiStok`/`StokBahan`; alternatif reservasi stok atomik saat pesanan dibuat (bukan saat dibayar) | TERBUKA |
| RISK-003 | Pembulatan diskon/pajak Int menyebabkan selisih 1 rupiah antar baris pada split bill | Pembayaran/Promo | TINGGI | SEDANG | Tentukan aturan pembulatan tunggal (mis. baris terakhir menyerap sisa) dan uji dengan kasus tepi | TERBUKA |
| RISK-004 | QRIS mode manual rawan human error (kasir salah tandai lunas padahal belum masuk) | Pembayaran | SEDANG | TINGGI | Wajib catatan referensi (`catatanKasir`) + audit log setiap konfirmasi manual; rencana migrasi ke gateway otomatis (ADR-003) | TERBUKA |
| RISK-005 | Performa WebView (Capacitor/Tauri) lebih lambat dari native murni, terutama di perangkat kasir lama | UI lintas platform | SEDANG | SEDANG | Uji perangkat kelas bawah sebelum rilis; batasi animasi berat di layar kasir/KDS | TERBUKA |
| RISK-006 | Dependency-cruiser rule (dapur read-contract, analitik read-model) dilanggar tanpa sadar saat refactor besar | Dapur/Analitik | RENDAH | SEDANG | `pnpm depcheck` wajib lolos di CI sebelum merge | TERBUKA |
| RISK-007 | Job agregasi read-model gagal/terlambat, dashboard analitik menampilkan data basi tanpa indikasi | Analitik | SEDANG | SEDANG | Simpan `dihitungPada` di setiap tabel `RM_*` dan tampilkan "data per {waktu}" di UI; alert jika job gagal >1 siklus | TERBUKA |
| RISK-008 | Approval bertingkat bisa dilewati lewat panggilan API langsung (bukan lewat UI) jika enforcement hanya di UI | Otorisasi | RENDAH | TINGGI | Semua approval WAJIB divalidasi di layer API/service, bukan hanya disembunyikan di UI | TERBUKA |
| RISK-009 | Multi-outlet: karyawan ditugaskan ke outlet yang salah menyebabkan data lintas outlet tercampur di laporan | Karyawan/Analitik | RENDAH | SEDANG | Validasi `PENGGUNA_OUTLET`/`outletUtamaId` konsisten sebelum agregasi laporan kinerja karyawan | TERBUKA |
| RISK-010 | Tabel append-only (mutasi stok, riwayat status, audit log) tumbuh tanpa batas, memperlambat query seiring waktu | Persediaan/Pesanan/Platform | TINGGI | SEDANG | Rencanakan strategi partisi/arsip periodik sejak awal (belum didesain - lihat MASTER-CHECKLIST) | TERBUKA |
| RISK-011 | Belum ada akses lingkungan Xcode/Android SDK untuk build & uji `apps/mobile` di sesi kerja ini | UI lintas platform | TINGGI (terjadi) | SEDANG | Build/uji mobile perlu dijalankan di mesin/CI yang memiliki toolchain terkait; dicatat sebagai keterbatasan lingkungan, bukan risiko produk | TERBUKA - KETERBATASAN LINGKUNGAN |
| RISK-012 | Model `Pengguna`/`Peran` saat ini (tenantId langsung, permission Json) membuat kebocoran data lintas tenant dan bypass permission jauh lebih mudah terjadi begitu implementasi dimulai (lihat DEFECT-LEDGER.md ALT-DEF-001, ALT-DEF-002, ALT-DEF-010) | Platform/Otorisasi/Multi-tenant | TINGGI | TINGGI | Perbaiki model `KeanggotaanTenant`/`KeanggotaanOutlet` dan normalisasi `Izin`/`PeranIzin` SEBELUM implementasi endpoint apa pun dimulai; tambahkan composite tenant-outlet constraint (ALT-DEF-010) dan test isolasi tenant wajib (ALT-DEF-027) sebagai gate sebelum merge domain pertama | TERBUKA |
| RISK-013 | Data transaksi (Pembayaran, Pesanan, TiketDapur, PromoPemakaian) berisiko korup/tidak bisa direkonstruksi karena state machine pesanan dangkal, TiketDapur/PromoPemakaian dibatasi unik per pesanan, dan tidak ada idempotency/outbox (lihat ALT-DEF-005, ALT-DEF-006, ALT-DEF-009, ALT-DEF-017) | Pesanan/Dapur/Promo/Platform | TINGGI | TINGGI | Perbaiki state machine 14-status, hapus constraint unik yang salah, tambahkan `IdempotencyKey`/`DomainOutboxEvent` sebelum endpoint checkout/pembayaran/dapur diimplementasikan | TERBUKA |
| RISK-014 | Alur QR ordering (menunggu persetujuan kasir), KDS multi-stasiun, dan promo stacking/BOGO tidak mungkin diimplementasikan dengan benar selama schema masih membatasi satu tiket dapur dan satu promo per pesanan serta state machine pesanan belum mendukung status menunggu-persetujuan (lihat ALT-DEF-005, ALT-DEF-006, ALT-DEF-009) | Pesanan/Dapur/Promo | TINGGI | TINGGI | Jadikan perbaikan ALT-DEF-005/006/009 sebagai prasyarat wajib sebelum sprint implementasi domain QR/Dapur/Promo dimulai, bukan dikerjakan paralel dengan asumsi schema saat ini sudah benar | TERBUKA |
| RISK-015 | Fase DEEP CORRECTION LOOP (dimulai 2026-07-25) menaikkan bar verifikasi secara signifikan - status `SIAP_DIVERIFIKASI` yang diberikan ke 25+ defect selama 14+ batch correction-loop sebelumnya SEMUANYA didasarkan pada premis "tidak ada Postgres nyata di lingkungan kerja", bukan pada verifikasi migrasi/trigger/test integrasi yang benar-benar dijalankan. Sekarang Postgres 16 nyata tersedia (`altora_resto_dev`, lihat ALT-DEF-029 versi diperbarui), bar yang sebelumnya dianggap "cukup" (validasi sintaksis + test struktur `node --experimental-strip-types`) tidak lagi memadai untuk klaim `DITUTUP` - risikonya adalah tim/reviewer keliru menganggap `SIAP_DIVERIFIKASI` dari batch-batch lama setara dengan "sudah diverifikasi terhadap Postgres nyata", padahal itu belum pernah terjadi untuk satu defect pun | Engineering/Proses | TINGGI (sudah terjadi sebagai asumsi implisit di ledger lama) | TINGGI | Jangan naikkan status defect mana pun ke `DITUTUP` hanya karena Postgres kini tersedia - wajib migrasi resmi (`prisma migrate deploy` sukses dari database kosong) DAN test integrasi nyata (bukan `--experimental-strip-types` struktur saja) yang benar-benar dijalankan terhadap `altora_resto_dev` sebelum status `DITUTUP` diberikan; audit ulang seluruh baris `SIAP_DIVERIFIKASI` di `DEFECT-LEDGER.md` terhadap bar baru ini sebagai bagian batch-batch berikutnya dari deep-correction-loop, mulai dari `ALT-DEF-044` (fold migrasi resmi) | TERBUKA |
| RISK-016 | Batch audit konsolidasi konkurensi (ADR-041) menemukan DAN memperbaiki 3 race condition nyata (ALT-DEF-051/052/053: kuota promo, reservasi stok, alokasi pembayaran) lewat proof dua-koneksi `pg` fisik. Ketiganya ditemukan HANYA karena satu batch mengaudit sistematis 5 kategori skenario (Migrasi/Tenant/Concurrency/Ledger/State machine) terhadap 13-17 file test yang sudah ada - **TIDAK ADA jaminan ketiganya adalah SATU-SATUNYA race yang ada.** ~30% dari 17 domain correction-loop (7 domain: Menu, Pembelian, sebagian Meja, Pelayan, Analitik, UI/UX, sebagian Security) TIDAK PERNAH mendapat audit dedicated sama sekali - domain-domain ini juga mengandung tabel dengan pola agregat/kuota/saldo yang secara struktural rentan race yang SAMA (`SELECT` lalu `UPDATE` tanpa lock, SUM lintas baris tanpa trigger pembanding) sebagaimana 3 yang sudah ditemukan. Menyimpulkan "hanya ada 3 race, semua sudah diperbaiki" dari cakupan audit yang PARSIAL adalah kesalahan penalaran yang harus dihindari eksplisit | Persediaan/Promo/Pembayaran/lintas-domain-belum-teraudit | TINGGI | TINGGI | JANGAN nyatakan concurrency-hardening "selesai" karena 3 ditemukan dan 3 diperbaiki - jadwalkan audit konkurensi dedicated untuk 7 domain yang belum tersentuh (pola yang sama: cari SUM/COUNT agregat lintas baris tanpa trigger pembanding, kolom kuota/saldo/stok yang di-update tanpa `SELECT ... FOR UPDATE`) sebagai bagian batch berikutnya, bukan diasumsikan aman karena "pola serupa sudah pernah diperiksa di domain lain" | TERBUKA - RESIDUAL, CAKUPAN AUDIT PARSIAL |
| RISK-017 | Job CI `lint` (`.github/workflows/ci.yml`, ditambahkan ADR-043) DIPASTIKAN gagal 100% dari waktu begitu dijalankan - `eslint` bukan devDependency di package.json manapun (root maupun 30 paket workspace) dan tidak ada satu pun `eslint.config.*`/`.eslintrc.*` di seluruh repo (ALT-DEF-056, dikonfirmasi lewat eksekusi langsung `npx eslint .` yang gagal nyata dengan "couldn't find an eslint.config file"). Job ini SENGAJA tetap dipasang di YAML (bukan dihapus) supaya statusnya jujur (merah, bukan hijau-palsu/di-skip diam-diam), dan SENGAJA tidak dijadikan gate wajib (job lain tidak `needs:` job ini) - tapi risiko operasionalnya tetap nyata: PR pertama yang membuka repo di GitHub akan melihat job merah tanpa konteks jika laporan ini tidak dibaca | Engineering/Tooling/CI | TINGGI (sudah terjadi, dikonfirmasi lewat eksekusi lokal) | SEDANG | Instalasi eslint + `eslint.config.mjs` sungguhan di root DAN diwariskan/direplikasi ke 30 paket workspace sebagai batch tooling tersendiri sebelum PR pertama dibuka ke reviewer eksternal; sampai itu terjadi, deskripsikan job `lint` merah ini secara eksplisit di PR description supaya tidak disalahartikan sebagai regresi kode | TERBUKA |
| RISK-018 | Pipeline CI (`.github/workflows/ci.yml`, 524 baris, 11+ job) ditambahkan batch ADR-043 tapi **belum pernah dijalankan oleh GitHub Actions sungguhan** sampai penulisan laporan ini - seluruh perintah individual di dalamnya (migrate deploy, test suite, depcruise) sudah terbukti bekerja saat dijalankan manual secara lokal terhadap `altora_resto_dev`, TAPI mekanisme orkestrasi CI itu sendiri (apakah `pnpm/action-setup` membaca `packageManager` dengan benar, apakah `pnpm install --no-frozen-lockfile` resolve bersih tanpa `pnpm-lock.yaml` ter-commit, apakah `services: postgres:16` lulus health-check dalam waktu wajar, apakah `turbo run lint`/`turbo run typecheck` menemukan script di seluruh 30 paket) TIDAK PERNAH diverifikasi lewat eksekusi runner nyata - lihat `RELEASE-EVIDENCE.md` bagian ADR-043 poin 9 untuk daftar lengkap 5 asumsi "plausible-but-unproven" | Engineering/CI | SEDANG | TINGGI | Jalankan workflow ini SEKALI secara nyata di GitHub Actions (push ke branch/PR percobaan) sebelum menganggap pipeline CI "berfungsi" - saat ini statusnya adalah desain yang belum dieksekusi, bukan bukti yang sudah diverifikasi | TERBUKA |
| RISK-019 | Ketersediaan Postgres 16 lokal (`altora_resto_dev`) yang menjadi dasar SELURUH klaim `DITUTUP` di deep-correction-loop (15 migrasi, 32 trigger, 39/39 test) terbukti benar HANYA di environment eksekusi sesi kerja ini - tidak ada bukti bahwa environment developer lain atau runner CI GitHub Actions akan punya Postgres yang sama-sama tersedia dan berperilaku identik (versi Postgres, locale/collation, extension terpasang). ALT-DEF-029 secara eksplisit TIDAK dinaikkan ke `DITUTUP`/`TIDAK_VALID` karena alasan ini - risiko ini murni mendokumentasikan ulang caveat yang sama di level risk-register supaya tidak hilang dari radar saat defect ledger dibaca sepintas | Engineering/Database/Lintas-environment | SEDANG | TINGGI | Verifikasi eksplisit lewat CI run pertama (lihat RISK-018) bahwa `services: postgres:16` GitHub Actions menghasilkan hasil migrasi+test yang IDENTIK dengan hasil lokal sebelum mengandalkan klaim "migrasi terverifikasi" di luar sesi kerja ini | TERBUKA |

## Catatan

- Semua entri di atas adalah risiko yang teridentifikasi pada tahap desain (belum ada
  kode produksi) - probabilitas/dampak akan dikalibrasi ulang begitu implementasi
  berjalan dan data nyata (mis. hasil load test) tersedia.
- Tidak ada risiko di atas yang berstatus `DIMITIGASI`/`DITUTUP` pada penulisan
  dokumen ini - status jujur mencerminkan bahwa mitigasi masih berupa rencana.
